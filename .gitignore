.claude/
.DS_Store
Thumbs.db
*.log
*.mp4
*.mov
# PNGs originais do ChatGPT ficam locais — só as versões otimizadas (img/) vão pro deploy
ChatGPT*.png
*.png
!cortex-frames/*.png
# Vídeos fonte ficam locais — só os frames vão pro deploy
*.mp4
*.mov

# Painel interno LinkedIn (uso local apenas)
linkedin/

# Documento Word interno (uso local apenas)
*.docx

# Materiais comerciais internos
comparativos/

# Stories Instagram (uso local)
stories/
.env
.env.local

# Backend
node_modules/
data/
*.db
*.db-journal
*.db-shm
*.db-wal

# Modelos 3D de teste pesados (106MB, ficam só local)
models/

# Arquivos PowerShell helpers (não vão pro repo público)
*.ps1.local
